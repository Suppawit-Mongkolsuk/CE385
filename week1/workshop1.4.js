const Username = "title";
const Password = "suppawit110";
const age = 19;

if (age <= 18) {
    console.log("อายุไม่ถึงเกณฑ์");
} else if (Username === "title" && Password === "suppawit1410") {
    console.log("เข้าสู่ระบบสำเร็จ");
} else {
    console.log("ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง");
}

